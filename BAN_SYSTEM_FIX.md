# แก้ไขระบบแบน (Ban System Fix) - 11 Feb 2026

## ปัญหาที่พบ

ระบบแบนไม่มีการตรวจสอบสถานะแบนในกระบวนการเข้าสู่ระบบ (Login) และการตรวจสอบการยืนยันตัวตน (Authentication) ดังนี้:

1. **Login Page** (`auth/login.php`) - ไม่มีการตรวจสอบว่าผู้ใช้ถูกแบนหรือไม่
2. **Auth Check** (`auth/auth_check.php`) - ไม่มีการตรวจสอบสถานะแบนของผู้ใช้ที่ล็อกอินแล้ว
3. **Database Initialization** (`utils/db.php`) - ไม่มีการตรวจสอบว่าผู้ใช้ถูกแบนอยู่หรือไม่

ผลมคือ: **แม้ว่าจะแบนผู้ใช้ในระบบแอดมิน แต่ผู้ใช้นั้นสามารถยังคงเข้าสู่ระบบได้**

---

## วิธีแก้ไข

### 1️⃣ แก้ไข `auth/login.php` - ตรวจสอบแบนตอน Login

**การเปลี่ยนแปลง:**
- เพิ่ม `ban_until` ในการ SELECT ข้อมูลผู้ใช้
- ตรวจสอบว่า `ban_until` มีค่าและยังอยู่ในช่วงแบน
- แสดงข้อความบอก user ว่าบัญชีถูกแบน และเหลือกี่วัน

**ตัวอย่างการทำงาน:**
```
❌ [ถูกแบน 30 วัน] → "บัญชีของคุณถูกแบนชั่วคราว สามารถล็อกอินได้ใน 25 วัน"
❌ [แบนถาวร] → "บัญชีของคุณถูกแบนอย่างถาวร"
✅ [ไม่ถูกแบน] → สามารถเข้าระบบได้ปกติ
```

---

### 2️⃣ แก้ไข `auth/auth_check.php` - ตรวจสอบแบนตอน Access Page

**การเปลี่ยนแปลง:**
- เช็คสถานะแบนของผู้ใช้จาก session
- ถ้า ban_until ยังอยู่ในช่วงแบน ให้ทำการ destroy session และ redirect ไป login
- แสดงข้อความแจ้งเตือนผู้ใช้

**ทำงาน:**
- ไฟล์ใด ๆ ที่เรียก `auth_check.php` จะตรวจสอบสถานะแบนด้วย
- ถ้าถูกแบน จะ logout โดยอัตโนมัติ

---

### 3️⃣ แก้ไข `utils/db.php` - ตรวจสอบแบนตอน Load User Data

**การเปลี่ยนแปลง:**
- เพิ่มการตรวจสอบ `ban_until` เมื่อโหลดข้อมูลผู้ใช้
- ถ้าผู้ใช้ถูกแบน ให้ destroy session และ redirect ไป login page
- ป้องกันการเข้าถึงหน้าใด ๆ จนกว่าแบนจะหมด

**ทำให้:**
- เพิ่ม layer ของความปลอดภัยหากผู้ใช้พยายามเยาะข้าม auth_check
- ป้องกันการเข้าถึง API endpoints

---

## ตรวจสอบและทดสอบ

### วิธีทดสอบระบบแบน:

#### ขั้นที่ 1: แบนผู้ใช้จาก Admin Panel
```
1. เข้า admin_dashboard/user_table.php
2. หาผู้ใช้ที่ต้องการแบน
3. คลิกปุ่ม "Ban" 
4. เลือกระยะเวลาแบน (30 วัน / 90 วัน / ถาวร)
5. ยืนยันการแบน
```

#### ขั้นที่ 2: ทดสอบ Login
```
1. Logout ออกจากระบบ
2. พยายามล็อกอินด้วยบัญชีที่ถูกแบน
3. ควรเห็นข้อความ: "บัญชีของคุณถูกแบนชั่วคราว สามารถล็อกอินได้ใน X วัน"
✅ สำเร็จ: ไม่สามารถล็อกอินได้
```

#### ขั้นที่ 3: ทดสอบ Access Page (ถ้ามีการโลหะลอบ)
```
1. หาก user พยายาม access หน้าอื่นด้วย session ที่ยังมี
2. เพจจะตรวจสอบสถานะแบน
3. Session จะถูก destroy โดยอัตโนมัติ
✅ สำเร็จ: ไม่สามารถเข้าหน้าใด ๆ ได้
```

---

## ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | คำอธิบาย | สถานะ |
|------|---------|------|
| `auth/login.php` | ตรวจสอบแบนตอน Login | ✅ แก้ไขแล้ว |
| `auth/auth_check.php` | ตรวจสอบแบนตอน Access Page | ✅ แก้ไขแล้ว |
| `utils/db.php` | ตรวจสอบแบนตอน Load User | ✅ แก้ไขแล้ว |
| `admin_dashboard/user_table.php` | จัดการการแบน | ✓ ไม่ต้องแก้ |
| `admin_dashboard/ban_history.php` | ประวัติการแบน | ✓ ไม่ต้องแก้ |

---

## ฟีเจอร์เพิ่มเติม

### 🔄 Automatic Unban (อนาคต)
สามารถเพิ่ม cron job เพื่อให้บัญชีที่แบนชั่วคราวสามารถเข้าใช้งานได้อัตโนมัติเมื่อหมดเวลาแบน:

```php
// SQL Query
UPDATE users 
SET ban_until = NULL 
WHERE ban_until IS NOT NULL 
  AND ban_until <= NOW()
  AND ban_until != '9999-12-31 23:59:59'
```

---

## สรุป

✅ **ระบบแบนประกอบด้วย 3 ชั้นของการตรวจสอบ:**
1. **Login Level** - ตรวจสอบตอนเข้าสู่ระบบ
2. **Auth Level** - ตรวจสอบตอน access page ที่มี auth_check
3. **DB Level** - ตรวจสอบตอน load data จากฐานข้อมูล

✅ **ผู้ใช้ที่ถูกแบนจะ:**
- ❌ ไม่สามารถเข้าสู่ระบบได้
- ❌ ไม่สามารถ access หน้าใด ๆ ได้
- ❌ ไม่สามารถใช้ API endpoints ได้

---

**แก้ไขเสร็จสิ้น - ระบบแบนจึงใช้งานได้อย่างถูกต้องแล้ว**
